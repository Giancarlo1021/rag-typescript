services:
  chromadb:
    image: chromadb/chroma:latest
    volumes:
      # Changed from /chroma/chroma to /chroma/data
      - ./chroma_data:/chroma/data 
    environment:
      - IS_PERSISTENT=TRUE
      - ANONYMIZED_TELEMETRY=FALSE
    ports:
      - "8000:8000"
    restart: unless-stopped